services:
  php:
    build: .
    container_name: globe-php83
    working_dir: /home/nginx/www/globe.uhb.ac.id
    volumes:
      - /home/nginx/www/globe.uhb.ac.id:/home/nginx/www/globe.uhb.ac.id
    depends_on:
      - redis
    networks:
      - globe-network
    ports:
      - "9001:9000"

  redis:
    image: redis:7-alpine
    container_name: globe-redis
    networks:
      - globe-network

networks:
  globe-network:
